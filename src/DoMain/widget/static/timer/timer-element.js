const getTimerElements = () => {
  elem = '<div class="widget widget-timer"><input type="hidden" class="timerHidden"><span class="event"></span><span class="remain-time"></span></div>';
  return elem
}